# 获取部门下所有子部门信息
organizationsChildren:
  coOrgId:
  expect:
    assert_type: errcode
    assert_result: '0'
# 获取公司组织架构
chart:
  coOrgId:
  expect:
    assert_type: errcode
    assert_result: '0'
# 修改公司信息
modify_companies:
  body:
    company_name:
    header_employee_id:
  coOrgId:
  expect:
    assert_type: errcode
    assert_result: '0'
# 获取层级结构
business_level:
  params:
    coOrgId:
    type:
  expect:
    assert_type: errcode
    assert_result: '0'
# 新增部门
departments:
  body:
    businessType:
    code:
    name:
    organizationBusinessLevelId:
    parent_id:
  expect:
    assert_type: errcode
    assert_result: '0'
