stop_apply:
  url: /dukang-workforce/api/workforce/stop/
  application_id:
  expect:
    assert_type: errcode
    assert_result: '0'
# 获取申请单列表
get_apply_list:
  params:
    offset: 0
    limit: 20
  body:
    coOrgId:
  expect:
    assert_type: errcode
    assert_result: '0'
#申请单详情页接口数据
apply_detail:
  application_id:
  expect:
    assert_type: errcode
    assert_result: '0'
# 获取需求单列表
get_require_list:
  body:
    coOrgId:
    limit: 20
    offset: 0
  expect:
    assert_type: errcode
    assert_result: '0'
# 需求单详情页接口数据
get_require_detail:
  id:
  expect:
    assert_type: errcode
    assert_result: '0'